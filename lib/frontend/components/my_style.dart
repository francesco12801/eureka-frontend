import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

const white = Color.fromARGB(255, 255, 255, 255);
const black = Colors.black;
const red = Colors.red;
const green = Colors.green;
const blue = Colors.blue;
const yellow = Colors.yellow;
const orange = Colors.orange;
const transparent = Colors.transparent;
const greenIOS = Color.fromRGBO(52, 199, 89, 1);
const greyIOS = Color(0xFFBFBFBF);
const lightGreyIOS = Color(0xFFD1D1D6);

// Defined colors

const primaryColor = Color.fromARGB(255, 17, 17, 17);
const cardColor = Color.fromARGB(20, 255, 255, 255);
const iconColor = Color.fromARGB(215, 255, 255, 255);
const selected = Color.fromARGB(255, 0, 183, 255);

// Defined Icons

const homeIcon = Icon(CupertinoIcons.home);
const notificationIcon = Icon(CupertinoIcons.bell);
const lightbulbIcon =
    Icon(CupertinoIcons.lightbulb, color: Colors.white, size: 30);
const networkIcon = Icon(CupertinoIcons.group);
const profileIcon = Icon(CupertinoIcons.person);
const filterUnreadIcon = Icon(
  CupertinoIcons.line_horizontal_3_decrease_circle,
  color: Colors.white,
);

// Defined Text Styles
const TextStyle normalWriteWhite =
    TextStyle(color: white, fontFamily: 'Roboto', fontSize: 15);
const TextStyle normalWriteBlack =
    TextStyle(color: black, fontFamily: 'Roboto', fontSize: 15);
